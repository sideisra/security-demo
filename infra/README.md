# Start
1. Start Infrastructure
   * `cd infra`
   * `yarn`
   * `yarn start`
2. Start backend
   * `cd backend`
   * `gradlew bootRun`
3. Start frontend
   * `cd frotnend`
   * `yarn`
   * `yarn start`
