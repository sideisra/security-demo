server:
  port: 8443
  ssl:
    key-store-type: PKCS12
    key-store: classpath:keystore/certAndKey.p12
    key-store-password: password
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8181/auth/realms/security-demo
#          TODO: check audience?
logging:
  level:
    org:
      springframework:
        security: INFO
