server:
  port: 8443
  ssl:
    key-store-type: PKCS12
    key-store: classpath:keystore/backend.p12
    key-store-password: password
    key-alias: sdbackend
security:
  oauth2:
    resource:
      jwk:
        key-set-uri: http://localhost:8181/auth/realms/security-demo/protocol/openid-connect/certs
      id: security-demo-backend
